<% if @plant %>
  <h1>Reviews for <%= @plant.name%></h1>
<% else %>
  <h1>All Reviews</h1>
<% end %>
<ul>
<% @reviews.each do |r|%>
  <li><%= r.user.name%> rates this plant <%= r.rating%> out of 5: <strong><%= link_to r.plant.name, plant_path(r.plant_id) %></strong></li>
<% end %>
</ul>